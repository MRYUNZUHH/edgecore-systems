"use client";import{useState}from"react";import{useBalance}from"@/lib/useBalance";import MobileNav from "@/components/layout/MobileNav";export default function Page(){const{balance,isLoggedIn,placeBet,addWinnings}=useBalance();const[show,setShow]=useState<"deposit"|"withdraw"|null>(null);const[amount,setAmount]=useState(100);const handle=()=>{if(show==="deposit")addWinnings(amount);else placeBet(amount);setShow(null);};return(<div className="min-h-screen flex flex-col"><main className="flex-1 pb-20 lg:pb-0 max-w-4xl mx-auto w-full p-4 space-y-6"><h1 className="text-3xl font-bold text-[#f0b429]">💰 Wallet</h1><div className="bg-[#13131f] border border-white/5 rounded-xl p-6"><p className="text-sm text-gray-400">Balance</p><p className="text-4xl font-bold text-white mt-2">${balance.toFixed(2)}</p><div className="flex gap-3 mt-4"><button onClick={()=>setShow("deposit")} className="bg-[#f0b429] text-black font-bold px-6 py-2 rounded-lg">Deposit</button><button onClick={()=>setShow("withdraw")} className="border border-[#f0b429]/30 text-[#f0b429] font-bold px-6 py-2 rounded-lg">Withdraw</button></div></div>{show&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 backdrop-blur-sm p-4" onClick={()=>setShow(null)}><div className="bg-[#13131f] border border-white/5 rounded-xl p-6 w-full max-w-md" onClick={e=>e.stopPropagation()}><h2 className="text-xl font-bold text-white capitalize mb-4">{show}</h2><input type="number" value={amount} onChange={e=>setAmount(Number(e.target.value))} className="w-full bg-[#0a0a0f] border border-white/10 rounded-lg text-white px-4 py-2 mb-4"/><button onClick={handle} className="w-full py-3 bg-[#f0b429] text-black font-bold rounded-lg">Confirm {show}</button></div></div>}</main><MobileNav/></div>);}