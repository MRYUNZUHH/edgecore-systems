"use client";import{useBalance}from"@/lib/useBalance";import MobileNav from "@/components/layout/MobileNav";const tiers=[{n:"Bronze",min:0,e:"🥉"},{n:"Silver",min:10000,e:"🥈"},{n:"Gold",min:50000,e:"🥇"},{n:"Platinum",min:200000,e:"💎"},{n:"Diamond",min:1000000,e:"👑"}];export default function Page(){const{totalWagered}=useBalance();const current=tiers.filter(t=>totalWagered>=t.min).length-1;const tier=tiers[Math.max(0,current)];const next=tiers[Math.min(4,current+1)];const progress=next?Math.min(100,((totalWagered-tier.min)/(next.min-tier.min))*100):100;return(<div className="min-h-screen flex flex-col"><main className="flex-1 pb-20 lg:pb-0 max-w-4xl mx-auto w-full p-4 space-y-6"><h1 className="text-3xl font-bold text-[#f0b429]">👑 VIP Club</h1><div className="bg-[#13131f] border border-white/5 rounded-xl p-6 text-center"><p className="text-6xl">{tier.e}</p><h2 className="text-2xl font-bold text-white mt-3">{tier.n}</h2><p className="text-gray-400">${totalWagered.toFixed(2)} wagered</p><div className="w-full bg-gray-800 rounded-full h-3 mt-4"><div className="bg-[#f0b429] h-3 rounded-full" style={{width:`${progress}%`}}/></div><p className="text-xs text-gray-500 mt-1">${totalWagered.toFixed(0)} / ${next?next.min.toLocaleString():"∞"} to {next?.n||"MAX"}</p></div></main><MobileNav/></div>);}