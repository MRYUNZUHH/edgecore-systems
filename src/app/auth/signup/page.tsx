"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; import { useStore } from "@/store/game-store"; import Link from "next/link"; export default function SignupPage() { const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const { login, isLoggedIn } = useStore(); const router = useRouter(); if (isLoggedIn) { router.push("/"); return null; } const handleSignup = () => { if (username.length < 3) { setError("Username too short"); return; } const result = login(username, password); if (!result.success) setError(result.error || "Signup failed"); }; return (<div className="min-h-screen flex items-center justify-center bg-[#080b12] p-4"><div className="w-full max-w-md card p-8"><div className="flex items-center gap-3 mb-6"><div className="w-10 h-10 bg-[#f5c842] rounded-xl flex items-center justify-center text-black font-bold text-lg">◆</div><h1 className="text-2xl font-heading font-bold text-white">Create Account</h1></div>{error && <p className="text-[#ef4444] text-sm mb-4 bg-[#ef4444]/10 p-3 rounded-lg">{error}</p>}<div className="space-y-3"><input value={username} onChange={e => setUsername(e.target.value)} placeholder="Username" className="input" /><input type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Password" className="input" /><button onClick={handleSignup} className="btn-primary w-full">Sign Up</button></div><p className="text-center text-[#5a6a85] text-sm mt-4">Already have an account? <Link href="/auth/login" className="text-[#f5c842]">Sign in</Link></p></div></div>); }